import { Poppins, Instrument_Sans } from "next/font/google"

const poppins = Poppins({ subsets: ["latin"], weight: ["700"] })
const instrum = Instrument_Sans({ subsets: ["latin"] })

export { poppins, instrum }
